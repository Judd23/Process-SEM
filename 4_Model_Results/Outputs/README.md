# Outputs (organized)

This folder contains the most recent pipeline outputs.

## Where to find things

- **SEM outputs (model runs)**
  - `A0_total_effect/`
  - `RQ1_RQ3_main/`
  - `A1_serial_exploratory/`
  - `RQ4_measurement/`
  - `RQ4_structural_MG/`
  - `RQ4_structural_by_re_all/`

- **Figures (PNG)**: `figures/` (Fig 1–Fig 12)
- **Tables (DOCX)**: `tables/` (e.g., `Bootstrap_Tables.docx`)
- **Standards dashboard + inputs**: `standards/`
- **Saved model syntax snapshots**: `syntax/`
- **Logs / checklists**: `logs/`
- **Sensitivity outputs**: `sensitivity/`

## Notes

- `run_log.txt` stays in the Outputs root so it’s easy to spot.
- Re-running the pipeline may re-create some files in the Outputs root depending on the generator scripts; if that happens, re-run the organizer step.
